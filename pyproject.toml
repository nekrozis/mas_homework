[project]
name = "mas-homework"
version = "0.1.0"
description = "A ReAct‑style agent prototype for academic literature processing: lists PDFs, extracts metadata (authors & affiliations), parses sections, analyzes themes and methods between papers, performs English abstract extraction and Chinese translation, and generates reading‑comprehension QA sets."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ollama>=0.6.0",
    "pydantic>=2.12.4",
    "pymupdf>=1.26.6",
]
